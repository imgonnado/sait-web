export enum StatusType {
  pending = 'pending',
  recruit = 'recruit',
  progress = 'progress',
  complete = 'complete',
  hold = 'hold',
}

export enum OnoffType {
  onoff = 'onoff',
  on = 'on',
  off = 'off',
}

export enum CategoryType {
  junior = 'junior',
  rapid = 'rapid',
  hobby = 'hobby',
  senior = 'senior',
}
